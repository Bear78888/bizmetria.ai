# Legacy Chat Briefs

**Status:** `LEGACY — PRESERVED FOR COMPATIBILITY`

The files in this directory are the recovered instructions for the former permanent-chat operating model. They remain available for history and existing links.

Canonical workstream instructions are now stored in:

`docs/workstreams/*/WORKSTREAM_BRIEF.md`

Rules:

- Do not update a legacy brief in parallel with its canonical workstream brief.
- Use the canonical workstream path for new assignments.
- Use `docs/control/START_WORKSTREAM_CHAT.md` to launch a temporary specialist chat.
- Legacy files are source material, not a second approved operating instruction.
- Remove legacy paths only in a separate future task after every reference is verified.
