# Supabase Edge Functions

This directory is reserved for versioned Edge Functions deployed by the native Supabase GitHub integration.

Core webhooks and server-only application logic may also use Next.js route handlers. A function is added here only when its execution or isolation requirements favor the Supabase runtime. No secret values belong in this directory.
